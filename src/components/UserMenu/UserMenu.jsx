import { Wrapper } from './UserMenu.styled';

const UserMenu = () => {
  return (
    <Wrapper>
      <p>Welcome, Dima</p>
      <button type="button">Logout</button>
    </Wrapper>
  );
};

export default UserMenu;
